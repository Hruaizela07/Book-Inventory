import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div className="bg-black text-white w-full h-screen ">
      ll
      <div>
        <Button>Home</Button>
      </div>
    </div>
  );
};

export default Home;
